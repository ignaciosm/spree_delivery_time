# Sample localization file for English. Add more files in this directory for other locales.
# See https://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  pickup:
  dropoff: "%{pickup_date}"
  shipment_mailer:
    shipped_email:
      pickup: "Pickup is %{pickup_date} between %{pickup_low} and %{pickup_high}"
      dropoff: "Dropoff is %{dropoff_date} between %{dropoff_low} and %{dropoff_high}"
  order_mailer:
    confirm_email:
      pickup: "Pickup is %{pickup_date} between %{pickup_low} and %{pickup_high}"
      dropoff: "Dropoff is %{dropoff_date} between %{dropoff_low} and %{dropoff_high}"
